version: "3.8"
services:
    redis:
        image: redis:latest
        container_name: redis
        restart: always
        ports:
            - "127.0.0.1:6379:6379/tcp"
        network_mode: "host"
